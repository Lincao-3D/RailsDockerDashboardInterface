<!DOCTYPE html>
<html>
  <head>
    <title>App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# Include application.css first %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <%# Conditionally include custom_login.css for the login page %>
    <% if controller_name == 'sessions' && action_name == 'new' %>
      <%= stylesheet_link_tag "custom_login", "data-turbo-track": "reload" %>
    <% end %>

    <%# Conditionally include dashboard_quick_img_tab.css for the dashboard page %>
    <% if controller_name == 'dashboard' && action_name == 'index' %>
      <%= stylesheet_link_tag "dashboard_quick_img_tab", "data-turbo-track": "reload" %>
    <% end %>

    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <%# Consider moving the Bootstrap CDN link above your custom stylesheets if custom_login needs to override Bootstrap %>
  </head>

  <body class="<%= 'login-page' if controller_name == 'sessions' && action_name == 'new' %> bg-dark text-white">
    <%= yield %>
  </body>
</html>